/**
 * The local package provides stateless security integration into spring
 * by supporting JWT tokens.
 *
 * This package does not require a warden server, the only requirement is that web requests
 * have a bearer token authentication header.
 */
package com.elderbyte.security.spring.local;